<UserControl x:Class="FBSI_Manager.EditDisplayControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FBSI_Manager"
             mc:Ignorable="d">
    <UserControl.Resources>
        <Style x:Key="Base" TargetType="Control">
            <Setter Property="MinWidth" Value="50"/>
            <Setter Property="Margin" Value="0,0,2,0"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        <Style x:Key="Labels" TargetType="Label" BasedOn="{StaticResource Base}">
            <Setter Property="Background" Value="DarkGray"/>
        </Style>
        <Style x:Key="TextBoxes" TargetType="TextBox" BasedOn="{StaticResource Base}">
            <Setter Property="TextWrapping" Value="NoWrap"/>
        </Style>
    </UserControl.Resources>
    <StackPanel Orientation="Horizontal">
        <Label Name="Info" Visibility="Collapsed" Style="{StaticResource Labels}"/>
        <Label Name="Display" Style="{StaticResource Labels}"/>
        <TextBox Name="Input" Visibility="Collapsed" Style="{StaticResource TextBoxes}"/>
    </StackPanel>
</UserControl>
